#include <stdio.h>
#include <stdlib.c> // needed for atoi/atof

int main(int argc, char *argv[])
{
    // arc -> number of arguments
    // argv -> array of strings (arrays) containing the arguments (2D array)

    if   (argc < 2) {
        printf("No argument! \n");
        return 1;
    }
    

}